apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tutorialbox-ingress
  namespace: tutorialbox
spec:
  ingressClassName: nginx
  rules:
  - host: tutorialbox.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 5000
